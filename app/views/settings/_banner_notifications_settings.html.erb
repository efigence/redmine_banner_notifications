<%= stylesheet_link_tag '/plugin_assets/redmine_banner_notifications/stylesheets/selectize.default.css' %>
<%= javascript_include_tag '/plugin_assets/redmine_banner_notifications/javascripts/selectize' %>
<p>
  <label><%= l('settings.allowed_groups_to_use_hamster') %></label>
    <%= select_tag "settings[banner_admins][]",
      options_from_collection_for_select(User.all.sort, 'id', 'name', @settings['banner_admins']
        ), :multiple => true, class: "selectize" %>

</p>
<p>
<style>
  div.box {
    overflow: initial;
  }
</style>
<script type="text/javascript">
  $(document).ready(function(){
    $('.selectize').selectize({
      plugins: ['remove_button']
    });
  });
</script>
